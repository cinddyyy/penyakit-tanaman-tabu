# Stable Core Dependencies
tensorflow==2.15.0  # Stable version to fix the Shape Mismatch bug
keras==2.15.0      # Keras version compatible with TF 2.15.0

# Remaining Application Dependencies (Pinned for consistency where possible)
streamlit==1.50.0 # Use the version visible in your Streamlit logs
mysql-connector-python
matplotlib
numpy
pandas
joblib
Pillow
scikit-learn
seaborn